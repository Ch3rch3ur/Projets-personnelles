# - name: Restart nginx
#  service:
#    name: nginx
#    state: restarted
#    enabled: true

- name: Reload nginx
  systemd:
    name: nginx
    state: reloaded

- name: Validate nginx config
  command: nginx -t
  register: nginx_test
  failed_when: nginx_test.rc != 0
  changed_when: false
